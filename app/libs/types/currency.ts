export enum RegularCurrency {
  EUR = 'eur',
  USD = 'usd',
}

export enum CryptoCurrency {
  TON = 'ton',
  USDT = 'usdt',
}

export enum RegularCharger {
  TONKEEPER = 'Tonkeeper',
}

export enum CryptoCharger {
  WALLET = 'Wallet',
  CRYPTO_BOT = 'CryptoBot',
}
